model: xception
input_size: 299
batch_size: 32
epochs: 1
learning_rate: 0.0001

num_workers: 16
class_weighted_loss: true

experiment_name: hufs_cluster_experiment
tracking_uri: file:./mlruns

seed: 42

# 🚨 데이터 경로 (프레임 뽑은 후 이 구조로 생성됨)
train_data_dir: /mnt/ssd/deeperforensics/frames/train
val_data_dir: /mnt/ssd/deeperforensics/frames/val
test_data_dir: /mnt/ssd/deeperforensics/frames/test

# 🚨 체크포인트/모델 저장 경로
checkpoint_dir: /mnt/ssd/project/outputs/checkpoints

